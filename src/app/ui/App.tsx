import { Header } from "@/common/components/header/Header";
import { Routing } from "@/common/routing";

export const App = () => {
  return (
    <>
      <Header />
      <Routing />
    </>
  );
};
